The index.js file deals with transforming the files into routes. This is a recursive process so:

A `routes/users/tasks` folder would become `/users/tasks` base endpoint.

Insise every route file there is a JSON that allows a custom syntax to be used for route definition as long as the route exporter on the bottom on the file is kept